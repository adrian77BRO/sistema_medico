/*import * as React from 'react';
import { createNativeStackNavigator } from '@react-navigation/native-stack';
import { PatientsScreen } from './screens/Patients';
import { PatientFormScreen } from './screens/PatientForm';
import { RootStackParamList } from './rootTypes';

const Stack = createNativeStackNavigator<RootStackParamList>();

export const PatientsNavigator = () => {
  return (
    <Stack.Navigator>
      <Stack.Screen name="PatientsScreen" component={PatientsScreen} options={{ title: 'Pacientes' }} />
      <Stack.Screen name="PatientFormScreen" component={PatientFormScreen} options={{ title: 'Formulario de Paciente' }} />
    </Stack.Navigator>
  );
};*/